import 'package:flutter/widgets.dart';

class LatestPostsDisplay extends StatelessWidget {
  const LatestPostsDisplay({
    required Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
